# Copy to .env, or just export these in your shell.

ANTHROPIC_API_KEY=sk-ant-...

# Optional
PATTA_MODEL=claude-sonnet-4-6
PATTA_MAX_EDGE=1024
PATTA_ALLOW_ORIGINS=*
PATTA_LOG_SCANS=0        # 1 enables the anonymous scan log (no photo, no location)
